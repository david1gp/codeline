# Chat area demo

## Goal
Add a polished T3code-inspired chat composer demo at `/demo/chatarea`, discoverable in `/demo`, that shrinks when scrolling up through messages.

## Decisions
- Follow the requested upward-scroll interaction, using the local T3code checkout as the visual reference.
- Keep this iteration in the demo; preserve production chat behavior.
- Reuse existing libraries and generic UI components. No new dependencies.
- Preserve drafts and keyboard accessibility when shrinking; focus/click expands the composer.

## Approach
Build a deterministic conversation demo with a reusable app-specific composer, bounded conversation scrolling, and a reduced-height composer state. Add the exact requested route and catalog link. Verify through the repository-managed combined preview, then commit and deploy with repository workflows.

## Tasks
1. Completed: implement the composer demo, scroll behavior, exact route, catalog entry, and focused tests.
2. Completed: browser-verify visuals and interactions on the managed combined preview; fix any issues.
3. Completed: run the commits skill with scoped conventional commits and push the changes.
4. Pending: deploy using the repository workflow and verify readiness.
